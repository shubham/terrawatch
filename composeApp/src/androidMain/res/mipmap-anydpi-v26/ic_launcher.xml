<?xml version="1.0" encoding="utf-8"?>
<!--
  Plan 4 Task 7: real adaptive icon (API 26+ - this app's own minSdk, so every installable device
  resolves this, never the legacy mipmap-*dpi/ic_launcher.png rasters - those exist purely as a
  compatibility fallback for tooling/launchers that read the flat mipmap directly; see
  task-7-report.md).
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
</adaptive-icon>
